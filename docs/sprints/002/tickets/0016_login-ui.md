# Login UI

## Description

Create a frontend login UI that can authenticate with the backend using the users created by the admin CLI.
Deploy the login UI to Azure production so users can sign in with their registered credentials.

## Acceptance

- A frontend login UI exists.
- The login UI can authenticate against the backend.
- The login UI works with users created by the admin CLI.
- The login UI is deployed to Azure production.

